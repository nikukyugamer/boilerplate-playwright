name: Boilerplate Playwright

on:
  workflow_dispatch:
  push:
    branches:
      - '**'
    tags-ignore:
      - 'v*.*.*'

jobs:
  yarn_install:
    uses: ./.github/workflows/_10_yarn_install.yml

  eslint:
    needs: yarn_install
    uses: ./.github/workflows/_20_eslint.yml

  yarn_build:
    needs: eslint
    uses: ./.github/workflows/_30_yarn_build.yml
